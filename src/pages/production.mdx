---
name: Production
route: /usage/production
menu: Usage
---

# Guide

To build the application in production mode, run:

```
npm run build
```

You can then run the optimized version of PizzaQL, by executing the following command:

```
npm start
```

